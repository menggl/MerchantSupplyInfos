server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/msi?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: Root@YMhmO=9iA5
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  sql:
    init:
      mode: never
  data:
    redis:
      host: host.docker.internal
      port: 6379
      database: 0
      password: Root@N8Hz@u#1-e
  jackson:
    default-property-inclusion: non_null
msi:
  member:
    default-days: 30
aliyun:
  oss:
    endpoint: ${ALIYUN_OSS_ENDPOINT:}
    bucket: ${ALIYUN_OSS_BUCKET:}
    accessKeyId: ${ALIYUN_ACCESS_KEY_ID:}
    accessKeySecret: ${ALIYUN_ACCESS_KEY_SECRET:}
    host: ${ALIYUN_OSS_HOST:}
wechat:
  miniapp:
    appid: ${WECHAT_MINIAPP_APPID:}
    secret: ${WECHAT_MINIAPP_SECRET:}
